
🔐 Simple Login / Signup System
A simple authentication system built with HTML, CSS, and Vanilla JavaScript using LocalStorage to manage user data.
📌 Description
This project implements a basic Sign Up / Login functionality on the client side.
User information is stored in the browser’s LocalStorage, and the system validates credentials during login.
It is designed for learning and practicing frontend authentication logic without a backend.
🚀 Features
Toggle between Login and Sign Up forms
Client-side form validation
Password visibility toggle
Store user data in LocalStorage
Login verification using stored credentials
Error handling and success messages
Clean and simple UI
🛠️ Technologies Used
HTML5
CSS3
Vanilla JavaScript
Browser LocalStorage API
⚙️ How It Works
🔹 Sign Up
User enters name, email, and password.
Form validation checks:
Required fields
Valid email format
Minimum password length
User data is stored in LocalStorage.
🔹 Login
Entered credentials are compared with stored user data.
If matched → login successful.
If not → error message displayed.
